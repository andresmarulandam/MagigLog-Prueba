const URL = import.meta.env.VITE_BACKEND_URL || 'http://localhost:3000';

const App = () => {
  return (
    <div>
      <h1 className="text-4xl font-bold ">Vamos Bien</h1>
    </div>
  );
};

export default App;
